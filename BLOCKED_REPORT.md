# BLOCKED_REPORT — OHMYTRIP 클론

> 이 환경(정적 export · 헤드리스 · 네트워크/권한 제약)에서 **직접 검증하지 못한 항목**과 사유를 정직하게 기록한다.
> "확인하지 못한 화면을 확인했다고 주장하지 않는다"는 원칙을 따른다.

---

## 1. 실사이트 대비 픽셀 비주얼 diff (하드 게이트: 평균 ≤2%, 핵심 ≤1%)
- **상태**: 미검증(BLOCKED)
- **사유**:
  - `www.ohmytrip.com` / `m.ohmytrip.com`은 **JS(Angular) SPA**로, 동일 상태·스크롤·Mock 고정 없이 실시간 캡처하면 배너·날짜·랜덤 이미지가 매번 달라져 안정적 비교가 어렵다.
  - 커밋된 기준 스크린샷(`tests/reference`)이 **비어 있음**(0장). 실사이트 라이브 캡처를 이 세션에서 수행/검증하지 않았다.
  - 인프라(`scripts/capture-reference.mjs`·`capture-clone.mjs`·`visual-diff.mjs`)는 존재하나 **라이브 베이스라인이 없어** diff 수치를 산출할 수 없다.
- **재개 방법**: 네트워크 허용 환경에서 지정 Viewport·고정 날짜·Mock 안정화 후 실사이트 캡처 → `tests/reference` 채움 → `visual-diff` 실행 → 수치 확보.

## 2. Lighthouse (하드 게이트: A11y·BP 95+, Perf 모바일85/데스크톱90)
- **상태**: 미검증(BLOCKED)
- **사유**: 이 세션에서 Lighthouse CI/러너를 실행하지 않았다. 정적 export를 로컬 서브로 띄워 측정 가능하나, 헤드리스 실행 결과를 확보하지 못했다.
- **참고(간접 증거)**: 콘솔 에러 0·img alt 0누락·버튼 접근성 이름 존재 → A11y는 95 근접으로 추정되나 **추정을 실측으로 주장하지 않음**.
- **재개 방법**: `npx lighthouse <url> --preset=desktop/mobile` 또는 `@lhci/cli`로 주요 라우트 측정.

## 3. E2E Critical 테스트 (하드 게이트: 100%)
- **상태**: 미충족 — **Playwright E2E 스펙 미작성**
- **현황**: `@playwright/test` 설치·`tests/visual` 구조 존재. 단, `tests/e2e`의 시나리오 스펙(호텔 검색 전체 흐름 등)은 없음.
- **재개 방법**: 호텔/항공 핵심 흐름·404·모바일 메뉴 스펙 작성 후 실행.

## 4. 서브페이지 모바일 레이아웃 (하드 게이트: 모바일 무가로스크롤)
- **상태**: 부분 미구현(코드 갭 — 환경 문제 아님)
- **현황**: 메인(홈·호텔·항공·액티비티·렌터카)은 `omt-mobile` 레이아웃으로 오버플로 0. 서브페이지(검색결과·로그인·회원가입·기획전·공지·FAQ·QnA·예약내역·약관·개인정보·룸타입·항공+호텔)는 데스크톱 트리만 존재해 모바일에서 가로 오버플로.
- **재개 방법**: 각 서브페이지의 `omt-mobile` 트리 구현(원본 `m.ohmytrip.com` 대응).

## 5. MCP/외부 커넥터
- Asana·Atlassian·Notion·Slack 커넥터는 이 세션에서 **미인증**이라 사용 불가. (본 작업과 무관)

---

## 요약
- **환경 제약(BLOCKED)**: 실사이트 픽셀 diff, Lighthouse 실측 → 수치 미확보.
- **코드 갭(구현 필요)**: 서브페이지 모바일 레이아웃, E2E 스펙.
- 위 항목이 남아 있으므로 **하드 게이트 전부 통과로 선언하지 않는다.**
